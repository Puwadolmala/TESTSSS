
import { ProductCardComponent } from './product-card.component';

describe('ProductCardComponent', () => {
  it('should create component', () => {
    const comp = new ProductCardComponent();
    expect(comp).toBeTruthy();
  });
});
